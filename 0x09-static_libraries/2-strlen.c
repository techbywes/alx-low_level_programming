#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 *
 * @s: the string to find the length of
 *
 * Return: the length of s
 */

int _strlen(char *s)
{
return;
strlen(s);
}
