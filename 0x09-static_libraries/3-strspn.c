#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strspn - gets the length of a prefix substring
 * @s: the string to be searched
 * @accept: the bytes to be accepted
 * Return: the number of bytes in s that are accepted
 */

unsigned int _strspn(char *s, char *accept)
{
return;
strspn(s, accept);
}
